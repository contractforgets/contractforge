# @contractforge/core

The core abstract parsing engine for the **ContractForge** enterprise SDK compiler. 

This package is framework-agnostic. It parses OpenAPI and Postman schemas into structurally perfect Typescript definitions.

## Installation
You generally do not need to install this directly unless building custom plugins. Use the main CLI!

```bash
npx @contractforge/cli init
```
